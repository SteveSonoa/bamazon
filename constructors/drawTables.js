var drawTables = function() {
	// Determine how many headers there are
	// For each header
	// Change the VARCHAR length to a fixed value like 16 or 20
	// Make the length of this part of the table this value
}